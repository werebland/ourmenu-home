import { system } from '@styled-system/core';
var config = {
  position: true,
  zIndex: {
    property: 'zIndex',
    scale: 'zIndices'
  },
  top: true,
  right: true,
  bottom: true,
  left: true
};
export var position = system(config);
export default position;
